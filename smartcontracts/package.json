{
  "name": "hibrido-contracts-uav",
  "version": "1.0.0",
  "description": "Fase 3 da Dissertação — Modelo Híbrido (FSM + UUPS Proxy) para missões UAV em rede Hyperledger Besu",
  "scripts": {
    "compile": "npx hardhat compile"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "dotenv": "^16.0.0",
    "ethers": "^6.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts-upgradeable": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.0"
  }
}
